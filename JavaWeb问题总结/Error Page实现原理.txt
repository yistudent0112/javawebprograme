Tomcat中Error Page的实现原理

1.首先我们在web.xml中配置了ErrorPage的错误状态码和对应的跳转页面
2。然后当项目被部署时，会被StandardContext(等于是项目的根Web容器)给加载
把对应的错误代码和错误跳转页面放到一个map中来做键值对的存放。
3.在之后的网页使用过程中，如果抛出了异常，会对应的产生一个异常状态码。
拿着这个异常状态码去之前设置过的错误匹配map中查找，
如果有值就返回之前设置过的页面
如果没有吗，为null，就重定向到tomcat默认的错误页面上去